const initialState = {};

const reducer = (state = initialState, action) => {
  switch (action.type) {
    case "CARDSTOSTATE":
      break;

    default:
      break;
  }

  return state;
};

export default reducer;
